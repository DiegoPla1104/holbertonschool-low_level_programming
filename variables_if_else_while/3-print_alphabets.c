#include <stdio.h>
/**
 * main - Alphabet
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	char ipa;

	if (ipa = 'a' ; ipa <= 'z' ; ipa++)
	{
		putchar(ipa);
	}
	putchar('\n');
	return (0);
}
