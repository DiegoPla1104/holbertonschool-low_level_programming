#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Alphabet
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	char ipa;

	for (ipa = 'a' ; ch <= 'z' ; ch++)
	{
	putchar(ipa);
	}
	return (0);
}
