#include "main.h"

/**
 *print_last_digit - Prints the last digit of a number
 *@r: is a variable
 *Return: On Success (0)
 */
int print_last_digit(int r)
{
	int n = (r % 10);
	{
		for (n < 0; n * -1; n==);
	return (0);
}
